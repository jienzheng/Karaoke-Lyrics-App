# FastAPI and Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# HTTP Client
httpx==0.27.2
requests==2.31.0

# Database
supabase==2.28.0
python-dotenv==1.0.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Spotify API
spotipy==2.23.0

# Romanization Libraries
pypinyin==0.51.0
pykakasi>=2.3.0
hangul-romanize==0.1.0

# setuptools provides pkg_resources (required by older pykakasi; kept for safety)
setuptools

# Dev / CI tools (not required at runtime — install locally or in CI)
# pip install ruff mypy
# ruff>=0.4.0
# mypy>=1.10.0

# CORS is built into FastAPI (fastapi.middleware.cors.CORSMiddleware)

# Date/Time
python-dateutil==2.8.2

# JSON
orjson==3.9.12

# Validation
pydantic==2.12.5
pydantic-settings==2.1.0
